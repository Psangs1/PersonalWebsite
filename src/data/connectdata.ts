import { projectdata } from "./projectdata"

export interface Connect {
    id: number,
    platform: string,
    icon: string,
    url: string
}

export const connectdata = [
{
    id: 0,
    platform: "Linkedin",
    icon: 
}

]